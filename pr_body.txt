Added a "gnat" toolchain and an example in `//integration`.
- Implemented `nix_gnat.bzl` that correctly wraps the GNAT toolchain standalone without tying it to `nix_cc.bzl`.
- Created an Ada `toolchain_type` in `gnat/BUILD.bazel`.
- Added a new `nix_gnat` module extension to configure the toolchain.
- Created an `ada_binary` rule to correctly locate and execute the compiler.
- Created `//integration/with-nix-gnat` example using `nix_gnat` attribute.

```
This pull request has been created by an automated coding assistant, with human supervision.
```

Prompt:
```
add a new feature in a new git worktree, based in ../: add a "gnat" toolchain and example in //integration
no the ada toolchain must be declared in MODULE.bazel, not as individual binaries
also ensure that gnat binaries are not tied to nix_cc.bzl but stand-alone
send pr
```
