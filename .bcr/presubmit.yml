matrix:
  platform:
    - ubuntu2004
    - macos
    - windows

tasks:
  verify_targets:
    name: "Verify targets"
    platform: ${{ platform }}
    build_targets:
      - "//..."
    test_targets:
      - "//..."
