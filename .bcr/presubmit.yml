bcr_test_module:
  module_path: "integration/with-nix-clang"
  matrix:
    platform: ["ubuntu2204"]
    bazel: [8.x]
  tasks:
    run_tests:
      name: "Run test module"
      platform: ${{ platform }}
      bazel: ${{ bazel }}
      test_targets:
        - "//..."
